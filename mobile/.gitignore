node_modules/
# Built web client copied in by bundle:web.
www/
# Native projects generated by `cap add` (regenerated from config).
/android/
/ios/
# Gradle output of local plugins.
plugins/*/android/build/

# Signing key config, if ever placed here (it lives outside the repo).
*.jks
*release*.json
